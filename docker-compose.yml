
services:
  train:
    image: train-schedule:${BUILD_NUMBER} # Use the build number to tag the image
    ports:
      - "80:8080" # Map container port 8080 to host port 8080 (adjust as needed)

